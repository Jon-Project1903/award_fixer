We are going to plan a web application that uses a local sqlite database. NPM or Python are fine choices you decide that. We have a few files that a user will need to upload. One will be a report of patents that issued along with inventor information. We will call this db_source in this discussion. db_source is a Patent: Patent: Asset Name Patent No. Title Issue Date of Patent Inventor: Legal Name Inventor: Office Location Country Inventor: Work Country ISO code Inventor: Address Inventor: Award Type One patent (same asset and patent no. and title) will have multiple rows depending on number of inventors. We need to compare the information in db_source with another file uploaded which we will call unified. The user should be able to create a project and name it (e.g. CY2025 Awards Review). The system will import all the records into different tables and begin a cross reference. We have a lot of things to consider. In the db_source and unified do we have the same number and the same inventors listed. Are the titles for the invention the same in both records. Are the inventor's names the same. Are there any patents in one file but not the other. Are the issued dates the same. If all the checks are correct, the status would be Passed Auto Review. If not, the status is Flagged. Each record in the review would be shown in a list view. Click on the row jumps to a detail view where we can reconcile the issues. So if inventors don't match up, we should have a way to line up the inventors side by side and choose the "correct information" for our final output report. We can talk about the final output report which we will use for ordering keepsakes later. Ask me questions. We should plan a couple of things: (1) a prompt to use in VS Code and (2) source code we can use to create this project on a mac using vs code

I want you to scaffold a full-stack project called PatentAwards:
- Backend: FastAPI + SQLModel + SQLite, with API endpoints for projects, import (CSV/XLSX), list patents, crossref/matching logic, reconciliation actions, and export CSV.
- Frontend: React + Vite (TypeScript optional) with pages: Projects list, Project detail (list of records), Patent detail reconciliation view.
- Add a Python import script that ingests `/mnt/data/db_source.csv` and `/mnt/data/unified.xlsx` into the SQLite database.
- Provide example SQLModel models for Project, Patent, Inventor, PatentCrossRef, ReconciliationChoice.
- Include a README with mac setup steps: python venv, pip install -r requirements.txt, npm install, how to run dev servers, and how to run the import script.
- Keep code minimal but production-structured (routers, services, models).
- Do not include any secrets or external services. Use local file saves and SQLite.
Scaffold the repository and show the important files: backend main.py, models.py, import_script.py, frontend src/pages/... and README.

When I import db_source and import unified, I want the data to go to different tables. I want a third reconciliation table. Each of those tables would have an fkey to the project. This lets me see in the db what is going in. Update the plan accordingly and then Convert the starter code above into a runnable scaffold repository and provide the full tree (backend + frontend). Implement the grouping logic to combine db_source rows by Patent No. and produce one Patent with multiple Inventor rows (I can do this immediately). Add a matching routine that computes match scores and populates PatentCrossRef status automatically and show an example CSV of flagged items. Build a simple React detail page demonstrating the inventor alignment UI.

Notice the format mismatch on patent number US12367625 vs US-12367625-B2, our code should match those based on the numeric middle part (12367625) convert the backend to a proper FastAPI + SQLModel app with the same tables and endpoints (projects, imports, list reconciliations, get reconciliation details, update reconciliation choices), and wire the React frontend to it (I can output the full backend files and updated frontend fetch code), or make the matching thresholds and weights configurable and add a small admin UI for tuning, or build the reconciliation detail page to display real data from the SQLite DB (i.e., replace the static demo with a live page). Create a project structure that I can open in VS Code and instructions in a GetStarted.md file

We should be creating a record for every line whether there is a match or not. If no match, we need to line up figure out whether to add a record for reconcilition or ignore the unified record or ignore the db_source record. Let's create a new matching_service.py and I will replace. We also need to style the front end to be modern, the look and feel is embarassing

We need a project selector page that we land on initially. We should be able to create new projects or select a project to review

In the reconiliation detail view. we need more ui. We need a way to close the detail view. We need labeling on which data is from Unified and which data is from our database (from Symfony). We need a way to create an reconciled record for each inventor (to the correct spelling, where we pick which data we use) and we need a way to chose which title and issue date to use. We need to recognize that Issue Date: 11/11/2025 and Pub Date: 2025-11-11T00:00:00 are the same date. In unified data, we need to split the inventors list up which are delimited by pipe.